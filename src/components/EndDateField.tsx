import { DateFieldClientProps } from "payload";
import EndDateFieldClient from "./EndDateField.client";

const EndDateField = ({ field }: DateFieldClientProps) => {
	// return <EndDateFieldClient field={field} />;
	return <p>Hello World</p>;
};

export default EndDateField;
